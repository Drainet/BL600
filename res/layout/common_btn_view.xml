<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/containerBtnConnect"
    style="@style/containerBtnConnect" >
    <Button
       android:id="@+id/btnConnect"
       style="@style/btnConnect" />
    <ProgressBar
        android:id="@+id/pbSpinnerReadingData"
	    style="@style/pbSpinnerReadingData" > 
    </ProgressBar>
</RelativeLayout>